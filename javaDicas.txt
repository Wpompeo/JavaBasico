####### JAVA - PROGRAMAÇÃO #######

* SAÍDA DE DADOS:
	
	* println(x) = imprime variável X e pula para nova linha.
	* printf( "%.2f%n", X) = imprime a varíavel X formata, onde .2f = número de casas decimais, %n = nova linha.
	* println( "Resultado: " + X + "Metros"); = Concatenação de impressão.
	* printf( "Resultado = %.2f metros%n", X); = Concatenação formatada.
	* printf( "%s tem %d anos e ganha R$ %.2f reais%n" , nome, idade, renda) = Concatenação de variaveis, onde %s = String, %d = double, %.2f = double
	* Locale.setDefault(Locale.US) = Define a utilização de "." e não "," para números float e doule.

#####################################################################################	

* CASTING:

	Gerar um resultado diferente do declaração de tipo da variável.
	Ex:
		double a;
		int b;
		
		a = 2.0;
		b = (int) a; ===> CASTING, atribui o valor float a uma variavel inteira.
		
#####################################################################################

* ENTRADA DE DADOS:

	import java.util.Scanner = biblioteca necessária para utilizar teclado
	
	Scanner sc = new Scanner(System.in); = Declaração variável"Scanner" para utilizar teclado.
	
	
	#LEITURAS:
	
	x = sc.next(); = ler Strings.
	x = sc.nextInt(); = ler números inteiros.
	x = sc.nextDouble(); = ler números double.
	x = sc.next().charAt(0); = ler um caractere.
	x = sc.nextLine(); = Ler palavra inteira da linha
	
	sc.close(); Quando não ou usar mais o teclado.
	
#####################################################################################

* FUNÇÕES MATEMÁTICAS:

	double x = 3.0;
	double y = 4.0;
	double z = -5.0;
	double A,B,C;
	
	A = Math.sqrt(x) = Atribuiu a variavel A o valor da raiz quadrada da variavel x. "sqrt" ==>retorna raiz quadrada.
	
	B = Math.pow(x, y) = Atribui a variavel B o valor da potenciação entre x, y.  "pow" ==>eleva a potenciação.
	
	C = Math.abs(y) = 4.0
	D = Math.abs(z) = 5.0 = Retorna o valor absoluto de um número, se tiver sinal negativo, sai o sinal. "abs" ==>retorna valor absoluto.
	
#####################################################################################

* CONDIÇÃO TERNÁRIA

	Sintaxe:
	( condição ) ? valor_se_verdadeiro : valor_se_falso
	
	Ex:
	( 2 > 4 ) ? 50 : 80 =====> Logo a saida será "80"
	
	( 10!= 3 ) ? "Maria" : " João" =====> Logo a saida será "Maria"
	
	*Pode ser utilizado para substituir IF/ELSE simples.

#####################################################################################
	
 *EXCEPTIONS
 
	Trhowable()
	Exception()
	OutOfMemoryError()
	
#####################################################################################

 *FUNÇÕES
 
	Formatar string: toLowerCase(), toUpperCase(), trim()
	Recortar string: substring(inicio), substring(inicio, fim)
	Substituir String: Replace(char, char), Replace(string, string)
	Buscar String: IndexOf, LastIndexOf
	str.Split("")
	
#####################################################################################

 *ESTRUTURA ORIENTAÇÃO OBJETO.

	Dentro da IDE utilizada para desenvolvimento, organize o projeto por pastas.
	
	Pasta "APPLICATION" ==> Coloque a classe main, classe responsável por iniciar o projeto.
		* Dados de entradas
		* Mensagens
		* Chamadas funções
		
	Pasta "ENTITIES" ==> Coloque a classe objeto, classe com as informações do código.
		* Atributos
		* Funções

#####################################################################################

 *MEMBROS ESTÁTICOS:
 
 Declarados sempre com letra maiscula: ex: public static double MEMBRES_NUMBER = 5;
 
 Uma função dentro da classe main, sempre tem que ter na sua declaração, o nome "static"
 Ex: 
 
	public static double calculaMembres(){
	}
 
 
#####################################################################################

 *MODIFICADORES DE ACESSO:
 
  Private: Só podes ser acessado na própria classe.
  
  Nada: O membro só pode ser acessado nas classes do mesmo pacote.
  
  Protected: O membro só pode ser acessado no mesmo pacote, bem como em subclasses de pacotes diferentes.
  
  Public: O membro é acessado por todas classes ( ao menos que ele resida em módulo diferente que não exporte o pacote onde ele está).
  
#####################################################################################

 *CLASSES SÃO TIPOS REFERÊNCIAS
 
 Variáveis cujo tipo são classes não devem ser entendidas como caixas, mas sim "TENTÁCULOS" (ponteiros) para caixas.
 
 Ex: Product p1, p2;
 
#####################################################################################

*DATA HORA

	*Data-hora Local
		LocalDate ====> sem time zone dd/mm/aa
		LocalDateTime ====>  com time zone, dd/mm/aa/hh
	
	*Data-hora global
		Instant ====> data com fuso
	
	*Duração
		Duration ====>  tempo duração
	
	*Outros
		Zoneld ====> 
		ChronoUnit ====> 
#####################################################################################